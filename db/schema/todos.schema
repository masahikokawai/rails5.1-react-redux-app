# -*- mode: ruby -*-
# vi: set ft=ruby :
create_table "todos", force: :cascade do |t|
  t.string "text", null: false
  t.boolean "completed", null: false, default: false
  t.datetime "created_at", null: false
  t.datetime "updated_at", null: false
end

# create table todos (id integer primary key, text varchar(256) NOT NULL, completed boolean DEFAULT false, created_at datetime NOT NULL, updated_at datetime NOT NULL);
